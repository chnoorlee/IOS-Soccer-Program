# SportsHub 比赛积分榜与历史交锋契约

> 冻结日期：2026-08-05  
> 当前实现级别：供应商中立的 HTTP 快照；真实供应商字段映射延后到获得沙盒和样例响应后。

## 为什么使用比赛作用域接口

- 客户端不得用赛事的 `currentSeasonId` 猜测一场历史比赛所属赛季。
- `GET /v1/fixtures/{fixtureId}/standings` 由服务端解析该场比赛的真实赛事和赛季，并把两者连同积分榜一起返回。
- `GET /v1/fixtures/{fixtureId}/head-to-head?limit=10` 独立返回该场两队最近的已结束交锋。积分榜和交锋分别请求、缓存、重试和显示状态，任一失败都不遮挡比分、时间线或另一标签页。
- 两个接口都是公开、可用 ETag 重验证的内容。网络不可用时可读取有时间标记的离线快照；仅在明确的 `remoteWithMockFallback` 模式下才可回退到带“演示数据”标记的虚构内容。

## 积分榜响应

响应必须包含 `fixtureId`、`competition`、`season`、`standings`、`source` 和 `updatedAt`。

1. `fixtureId` 必须等于请求路径中的 ID；`competition.id` 必须等于当前比赛快照的赛事 ID。
2. `season` 是服务端确认的比赛赛季。客户端只显示该值，不从当前日期或 `currentSeasonId` 推断。
3. `standings` 为空表示该赛事/赛季没有可用积分榜；不得伪造零值排名。
4. 非空积分榜中，每组按 rank 严格升序，同组 rank 和 team ID 唯一；所有组的 team ID 也必须唯一。
5. 每行必须满足 `played = won + drawn + lost`，计数非负，最近状态最多 5 场。积分可因纪律扣分而为负。
6. 非空积分榜必须同时包含当前比赛两队；否则视为赛事/赛季错配，不展示部分表格。

## 历史交锋响应

响应必须包含 `fixtureId`、当前比赛的 `homeTeam`、`awayTeam`、`meetings`、`source` 和 `updatedAt`。

1. `limit` 范围为 1...20；客户端默认请求 10。
2. 两支球队必须与当前比赛快照的主客队 ID 一致。
3. `meetings` 只包含已结束且比分完整的比赛，排除当前比赛，稳定 ID 不得重复。
4. 每场历史比赛的球队集合必须恰好等于当前两队；主客场可以互换。
5. 历史比赛按 `(kickoffAt 降序, id 升序)` 排列，条数不得超过 `limit`。
6. 历史交锋允许跨联赛、杯赛等赛事；每张卡片必须显示赛事名称，避免把跨赛事样本误解为同一联赛走势。
7. 客户端只汇总当前返回窗口内的胜/平/负，并明确显示“最近 N 场”；不把历史结果写成预测、概率或因果判断。

## 页面状态与辅助功能

- 新增“积分榜”和“历史交锋”两个比赛中心标签页，各自拥有 idle/loading/loaded/empty/error 状态和至少 44pt 的重试按钮。
- 用户切换标签后才发起对应请求；成功结果在当前页面生命周期内保留。下拉刷新只刷新当前标签的上下文，同时不清空另一标签的可读结果。
- 普通字号使用可横向滚动的积分表；辅助功能字号切换为纵向卡片，避免强制缩小文本。
- 当前比赛两队同时使用可见图标和 VoiceOver 文本标识，不能只靠底色。最近状态同时显示本地化的胜/平/负字母和颜色。
- 错误、空结果、数据来源、更新时间以及“跨赛事”范围均以文字呈现；失败后把辅助功能焦点移到对应错误状态。

## 本切片验收

- 单元测试覆盖比赛/赛事/球队错配、赛季显式返回、积分榜乱序或重复、算术不一致、H2H 跨赛事允许、非目标球队拒绝、未完赛拒绝、重复/乱序/超限拒绝。
- Remote provider 测试覆盖两个 URL、`limit=10`、ETag 缓存路径和资源级新鲜度记录。
- Mock 数据明确标注为虚构；弱网回退状态不能冒充真实供应商数据。
- UI 自动化旅程至少能定位六个比赛中心标签，并验证积分榜和历史交锋内容入口。
