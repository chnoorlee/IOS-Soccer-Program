# SportsHub 分步实施路线

本路线把“最终包含四款参考 App 的公开能力”与“每一步可以独立验收”同时满足。每一阶段只有在其验收闸门通过后才进入下一阶段。

## 当前进度（2026-08-08）

- 阶段 0：已以“暂定、可逆”状态记录在 `DECISIONS.md`，仍等待用户对品牌、语言、Mac 和数据预算最终确认。
- 阶段 1：工程配置、设计系统、Arabic/English、RTL/LTR、Widget/Live Activity Extension、双目标 App Group 配置和测试目标源码已建立。
- 阶段 2：模拟数据纵向切片源码已建立，覆盖引导、关注、首页、比赛列表、比赛中心、新闻、探索和个人设置。
- 阶段 3（部分）：OpenAPI 3.1 已覆盖 55 条路径、69 个操作和 148 个 Schema；iOS 已实现首页、按日赛程/比赛详情、比赛作用域积分榜/H2H、比赛时刻/赛后报道、赛事赛季赛程、赛事/球员近期内容、全局转会中心、赛季重要日期、关注球队上一场/下一场批量看板、免费小组排名预测、文章/结构化 Visual Brief、卡片公开互动快照、授权主图与审核式文章社区、视频编辑发现、节目/分集中心与授权海报、搜索、球队/球员详情、赛季阵容、球员转会记录、赛事积分榜、榜单、观看进度、观看历史、文章/视频收藏、球队/球员/赛事关注、账户会话、账户通知偏好和 APNs 设备注册的 DTO 映射、HTTPS `/v1` 配置、ETag、最近成功缓存及受控 Mock 回退。
- 阶段 4（部分）：Explore 已从首页数据筛选/视频占位升级为独立文章、视频、球队和赛事数据源，并在四类网格上方提供节目中心、全局转会中心与赛季重要日期三个发现工具入口；全局搜索具备 2–100 字符门禁、350ms 防抖、请求身份防竞态、Arabic 正规化的确定性 Mock 匹配、服务端顺序保持、结果摘要与载荷驱动类型范围。Explore 另具备文章正文/更正/撤回状态、视频权利状态、双语详情页，以及由专用发现快照驱动的精选主视觉、显式 Trending 名次、运动类别与全部/直播/回放/集锦/原创/采访频道；节目库按运动重新请求完整目录并进入显式分集关系。
- 阶段 5（部分）：已实现不在启动时弹框的通知权限流程、拒绝后跳转系统设置、九类账户级事件偏好（黄牌、红牌、换人独立控制并兼容旧 `card` 响应）、每次活动态重新向 APNs 注册、令牌轮换的串行上传、Debug/Release APNs 环境配置，以及退出登录撤销刷新令牌族设备绑定的 API 契约。球队、球员、赛事详情和比赛中心现有统一情境入口；它只按复合关注身份或共享比赛匹配器解释受众资格，复用全局事件偏好，关注时不请求权限。偏好和设备注册均为 Bearer、`no-store`、幂等请求且不会回退 Mock；Widget/Live Activity 的本地生命周期源码也已完成，但尚缺真实推送后端、证书/签名、服务端事件去重与限流、ActivityKit 推送令牌链路和真机运行验证。
- 阶段 6（部分）：已实现球队、球员和赛事纵向详情；赛事新增不依赖赛季的“最新”，并以 Provider 发布的新到旧历史赛季档案驱动其余三个按需加载标签，展示逐赛季隔离的分组积分榜、进球/助攻/红黄牌榜和完整赛程。目录有 50 项上限，会拒绝重复、乱序、无效日期和当前标志冲突；不推断缺失赛季或宣称档案完整。球员近期内容位于统计与转会之间；全局转会中心独立分页并显式区分已完成、已达成和传闻；赛季重要日期以 Provider 原子窗口区分即将发生/完整赛季及载荷真实类型。两类编辑频道均为服务端显式关联，新闻/视频分轨且失败独立；赛程保留直播、未开赛、已结束、延期和取消状态，并用原 fixture ID 进入比赛中心。积分榜、转会轨迹、赛季档案和赛季时间线在辅助功能字号下改为纵向卡片，标签/筛选控件改为纵向 44pt 按钮；排名、赛果、状态和榜单值均不只依赖颜色表达。
- 阶段 7（部分）：已实现短时播放会话 POST、显式设备能力、HTTPS/过期/DRM 契约校验及 AVKit 播放器。播放器使用系统控件，不自动播放；字幕和音轨交给系统菜单，AirPlay 与画中画由服务端逐会话授权。点播会在媒体就绪后恢复位置，每 10 秒合并保存、播完标记完成并在关闭时尽力冲刷；直播不进入续播。Explore 展示继续观看和已保存视频；Following 独立聚合通知设置、已存文章、已存视频和球队/球员/赛事关注，并为最多 100 支关注球队批量展示可空的上一场/下一场，超限球队仍保留基础入口；三类详情页均提供 44pt 关注入口，混合列表使用类型+ID 复合身份和确定性排序。游客关注保存带类型快照，账户响应要求服务端权威快照；缺失快照的旧记录仍可移除。关注只定义通知候选受众，不会自动开启全局事件偏好或请求系统权限。个人页提供包含已完成项目的观看历史、单条删除与清空确认、失败重试和删除后焦点迁移。游客文章/视频状态通过文件保护持久化并可离线读取；账户模式已加入 Sign in with Apple、Keychain、旋转令牌单飞刷新、失败关闭的数据路由、文章收藏与多实体关注同步，以及包含文章收藏和关注对象的确认式游客合并。隐私页现可统计并清除本机游客历史、文章/视频收藏、关注和视频快照，同时隔离账号状态及旧关注快照。尚缺真实身份后端运行验证和 FairPlay 内容密钥处理。
- 阶段 6/7 公共数据状态切片：首页、赛程/比赛中心、新闻和视频列表/详情已显示在线更新、ETag 已验证、离线快照、账户私有实时、纯演示、虚构演示回退和刷新失败；成功状态只在解码与领域契约通过后记录。状态具备双语文案、文字加图标表达、相对时间和刷新后的 VoiceOver 焦点；账号变化及并发请求采用最后一次请求生效，旧身份数据不会回写当前页面。
- 阶段 3 比赛中心增量切片：已冻结单场全局 revision、稳定事件 ID、更正/tombstone、排他游标和恢复策略；客户端实现无缓存 HTTP 增量轮询、确定性 reducer、比分与赛况合并、前后台/手动刷新恢复、退避、终止条件、双语连接状态及重要事件 VoiceOver 公告入口。真实比赛失败不会混入 Mock；SSE 与供应商字段映射等待沙盒证据。
- 阶段 3/6 比赛上下文切片：比赛中心已补齐积分榜与历史交锋两个标签和独立加载/空/错误/重试状态。积分榜由比赛作用域响应显式确认赛季并严格校验排名、球队与场次数学；H2H 限定已结束的目标球队对、稳定倒序且允许带赛事标签的跨赛事样本。两者均显示来源/更新时间/演示或离线边界，大字号积分表切换为卡片；真实供应商映射仍等待沙盒样例。
- 阶段 3/7 比赛内容切片：冻结规则见 `FIXTURE-CONTENT-CONTRACT.md`。比赛摘要现独立加载 `/fixtures/{fixtureId}/content`，精确校验比赛回显、最多 10 个时刻/10 篇报道、唯一时刻/视频/文章 ID、0...200 可空分钟和逐条播放权后才缓存。常规字号使用分钟徽章横向轨道，辅助功能字号切换为纵向；内容错误不遮挡比分、事件、阵容、统计或上下文标签，Mock 不生成媒体或播放授权。真实 CMS、版权窗口、海报和比赛日供应商 SLA 仍待外部证据。
- 阶段 3/4 分享与深链切片：比赛、文章、视频、球队、球员和赛事详情均使用原生系统分享入口；统一路由严格限制可信 HTTPS 源、固定路径、单次解码和安全 ID。链接在引导完成前排队，随后替换目标 Tab 的深链路径。公开基地址默认留空，当前只分享文本；自有域名、AASA、Web/App Store 回退和真机 Universal Links 仍是发布闸门。
- 阶段 2/4 首页可解释聚合切片：Home 直接展示三类关注对象；只按参赛球队或赛事 ID 形成带原因的相关比赛，球员关注不推断赛程，其他比赛保留为公共重要赛程，文章明确标为最新新闻；账号切换同步完成前不暴露旧身份兴趣。
- 阶段 2/4 首页新闻发现切片：最新新闻现提供“全部/已收藏”范围、仅由当前载荷真实分类键生成的筛选，以及不附加热门/精选语义的首条视觉层级。收藏加载、空态和失败与公开首页隔离；收藏变更会重载，身份切换会清空旧收藏并使在途请求失效；辅助功能字号改用纵向 44pt 控件。
- 阶段 3/4 文章 Visual Brief 切片：冻结规则见 `ARTICLE-VISUAL-BRIEF-CONTRACT.md`。文章格式与详情结构显式配对；1...4 个区块支持指标网格、恰好两项对比和序列，数量、跨区唯一项目 ID、双语长度及控制字符在公共缓存前校验。旧收藏和滚动 API 缺失格式时迁移为普通文章。Mock 五个数字明确为虚构；UI 使用语义文本、可见数据说明、RTL leading/trailing 和辅助功能字号单列重排。真实 CMS、授权统计与视觉发布审核仍待外部系统。
- 阶段 3/4 文章卡互动摘要切片：冻结规则见 `ARTICLE-ENGAGEMENT-SUMMARY-CONTRACT.md`。文章列表与详情摘要可携带 0...20 亿的总反应数和已发布评论数；旧缓存/滚动接口缺失时隐藏，负数、溢出和损坏持久值失败关闭。标准/首条卡片均用原创“赛事脉冲条”展示本地化完整短语，辅助功能字号纵向重排，整卡仍是单一导航目标。快照使用公共 ETag，个人反应与详情社区保持 `no-store`；分享次数、卡片内分享操作、热度排序和实时承诺仍等待真实聚合/归因契约。
- 阶段 3/4 文章授权主图切片：冻结规则见 `ARTICLE-HERO-MEDIA-CONTRACT.md`。文章列表与详情可携带显式对象或 `null`；DTO 在缓存前校验稳定 ID、HTTPS 直链、精确 MIME、尺寸/像素/比例和双语替代文本/署名。独立无身份媒体会话使用精确主机 allowlist、拒绝重定向和 Cookie、8 MiB 流式上限，并在 UIKit 解码前核对真实像素。卡片图片为装饰，详情显示替代文本、署名和失败重试；Mock 只用原创封面，个人收藏不保存签名 URL。真实 CMS 权利证明、CDN、裁切编辑和 Apple 运行时视觉/VoiceOver 仍待外部系统与 macOS/Xcode。
- 阶段 2 首页比赛筛选切片：Home 用一个状态控件同时过滤关注相关和公共比赛；直播合并进行中/中场，其他状态精确匹配，只显示载荷实际存在的选项。过滤保留比赛 ID、关注原因、分区和 Provider 顺序，失效状态在刷新提交前归一化为全部；辅助功能字号改为纵向 44pt 控件。
- 阶段 2 比赛发现切片：Matches 升级为以今天为中心的五日日期轨、全部/直播与载荷驱动赛事筛选；比赛按赛事首次出现顺序分组并保留组内 Provider 顺序，分组标题进入赛事详情。Mock 现包含两个可发现且能加载积分榜/榜单的赛事，用于验证组合筛选与赛事作用域空态；大字号把三组控制重排为纵向 44pt 按钮。
- 阶段 2 比赛日期/搜索切片：Matches 顶部已补齐官方截图中的日历和搜索入口。任意日期按本地自然日与 DST 安全的日历算术重新居中五日轨；搜索只作用于当前日期、状态和赛事筛选后的比赛，覆盖双语队名/缩写、赛事和场馆并保留原 ID/顺序。原生 DatePicker、明确搜索状态和 44pt 操作均具备双语及稳定辅助功能标识。
- 阶段 2 关注比赛切片：Matches 新增固定“全部/关注”范围，只按显式关注球队（主队或客队）和赛事 ID 匹配，并逐场说明球队、赛事或两者同时命中的原因；球员关注不推断赛程。筛选与日期、直播、赛事和搜索组合，赛事轨仍来自整日载荷。账号切换会立即禁用关注结果、作废旧同步并回到全部，避免跨身份泄漏；缺少数据排名证据的 Top/热门范围未被伪造。
- 阶段 3/6 赛事赛程切片：冻结规则见 `COMPETITION-FIXTURES-CONTRACT.md`。新增显式赛事+赛季接口，最多按 100 条分页聚合 1,000 场；每页在写入缓存前校验范围回显、全局时间/ID 顺序、全局唯一性、页大小和游标终止语义。后续页故障不会返回部分真实列表，只可整份回退到具名 Mock。纯模型测试覆盖六种状态的完整互斥分组及正反序，Remote 测试覆盖两页成功、错配不缓存、跨页乱序/重复拒绝和后续页故障整份回退；UI 旅程覆盖赛事详情切换赛程并打开比赛中心。Swift 类型检查、XCTest、RTL/大字号布局和 VoiceOver 仍待 macOS/Xcode。
- 阶段 4/7 视频发现切片：冻结规则见 `VIDEO-DISCOVERY-CONTRACT.md`。新增原子 `/videos/discovery` 快照，由服务端明确下发最多 100 条带 sport 的视频、可空精选 ID 和最多 10 条有序 Trending ID；客户端在缓存前校验唯一、引用完整及精选/热榜互斥，不用观看量或个人状态推断名次。资料库按 sport+type 交集筛选并保留 Provider 顺序、原 ID、可播放性和限制原因；辅助功能字号把控件和排名卡纵向重排。旧 `/videos` 分页完整性仍保留。Exclusive、观看数和个性化推荐继续等待权威字段。
- 阶段 4/7 视频授权海报切片：冻结规则见 `VIDEO-POSTER-MEDIA-CONTRACT.md`。视频列表/发现/详情可携带显式对象或 `null`；DTO 在缓存前校验稳定 ID、HTTPS 直链、MIME、尺寸/像素/比例和双语替代文本/署名。它复用无身份、精确主机 allowlist、拒绝重定向/Cookie、8 MiB 流式上限及 ImageIO 解码前核对的公共图片管线。标准、精选、Trending 与详情采用原创 SportsHub 取景框；卡片图为装饰，详情提供替代文本、署名和 44pt 重试。海报不改变 `isPlayable`，个人视频状态不保存签名 URL，Mock 无第三方图片。真实 CMS 权利证明、CDN、裁切编辑与 Apple 运行时视觉/VoiceOver 仍待外部系统及 macOS/Xcode。
- 阶段 4/7 节目与分集中心切片：冻结规则见 `VIDEO-PROGRAM-HUB-CONTRACT.md`。新增 `/video-programs` 与 `/video-programs/{programId}`，目录和详情分别使用独立公共新鲜度、1...50 项分页、最长 2,048 字符不透明游标、Provider 顺序、页内/跨页唯一 ID 与路径节目回显。Explore 入口使用原创深墨“节目架”和运动信号轨，常规字号双列、辅助功能字号单列；视频详情中的节目变成可导航关系。首屏恢复性故障只允许整份具名 Mock 且终止分页，后续页只重试；节目没有 Logo、主持人、季数、排播、赞助或播放授权字段。真实节目目录、媒体关系与权利仍待授权 Provider，Swift/XCTest/RTL/VoiceOver 仍待 macOS/Xcode。
- 阶段 3/4 Arabic-first 全局搜索切片：冻结规则见 `ARABIC-SEARCH-CONTRACT.md`。远程响应最多 100 条且 type+entityId 唯一，客户端 All 范围严格保留 Provider 相关性顺序，类型范围只保留有序子序列。Mock 去除 Arabic 变音/tatweel、统一 Alef 变体与 Alef Maqsura，再用四级确定性匹配；不音译、不混同 Taa Marbuta。页面新增比分牌式查询/已加载计数、动态类型范围轨、旧响应隔离、错误焦点和 VoiceOver 完成公告。游标翻页、热搜、历史、纠错建议与个性化排序仍等待独立产品/隐私契约。
- 阶段 3/7 关注球队赛程看板切片：冻结规则见 `FOLLOWING-TEAM-DASHBOARD-CONTRACT.md`。Following 按关注顺序用公共批量资源加载每队可空的上一场/下一场，HTTP 每批 20、逻辑总量 100，超过上限仍保留基础球队卡；响应在缓存前校验数量、顺序、球队回显、比赛状态、唯一归属和快照一致性。任何批次失败都不提交部分列表；混合批次按最差新鲜度汇总，避免离线缓存被另一批的在线验证掩盖。卡片提供球队、取消关注和原比赛中心入口，并在辅助功能字号下纵向重排。真实广播排期和 Trending 仍等待供应商字段与版权证据。
- 阶段 1/5 Widget 与 Live Activity 切片：冻结规则见 `WIDGET-LIVE-ACTIVITY-CONTRACT.md`。下一场 Widget 只消费 App Group 中版本化、长度受限的最小公共快照；选择器仅匹配关注球队/赛事，直播优先且无结果即清空，绝不回退固定虚构比赛。比赛中心提供用户主动启动/停止、开球前四小时资格门禁、同场去重、比分/语言/演示/过期状态更新和终场 dismissal；四小时赛前窗口为 ActivityKit 八小时活动总寿命预留另一半比赛时间。`pushType: nil` 与界面文案共同声明当前只支持比赛页前台驱动更新。App Group、锁屏、Dynamic Island、StandBy、外部移除、VoiceOver 和 Arabic RTL 仍待真实开发者签名与真机验证。
- 阶段 3/4 赛季重要日期切片：冻结规则见 `SEASON-CALENDAR-CONTRACT.md`。新增原子 `/season-calendar` 快照，显式提供最多 400 天窗口、来源、更新时间和最多 200 个 Provider 事件；客户端校验唯一 ID、开始/结束边界、最长 120 天区间、时间/ID 顺序、赛事快照一致性和文本上限后才缓存。Explore 使用原创纵向赛季脊线，支持即将发生/完整赛季和载荷真实类型筛选；赛事事件沿原 competition ID 导航，非赛事事件保持只读。恢复性故障只整份切换为具名 Mock，绝不拼接真实与虚构事件。提醒、票务、个人日历写入和真实 Provider 纠错 SLA 继续等待独立契约。
- 阶段 8 文章社区切片：冻结规则见 `ARTICLE-COMMUNITY-CONTRACT.md`。文章详情已有三类反应、审核式评论、举报和作者屏蔽的双语界面与 API 源码；公共列表只接受已发布评论，个人读写绑定账号且使用 Bearer `no-store`，任何社区请求均不走公共缓存或 Mock 故障回退。配置默认关闭，只有登录、真实 HTTPS 社区规范和发布方支持入口同时满足才开放写入。已部署审核队列、过滤、封禁/申诉、支持 SLA 与 App Store UGC 证据仍是生产硬闸门。
- 阶段 8 订阅/去广告切片：冻结规则见 `SUBSCRIPTION-AD-FREE-CONTRACT.md`。Profile 已接入 StoreKit 2 的动态月/年商品、当前权益、交易更新、购买、显式恢复和 Apple 系统管理入口；只接受配置产品的已验证有效交易，价格不硬编码。发布配置默认留空、广告默认关闭，Debug UI 预览具名且不进入 Release 行为。App Store Connect 产品、正式法务链接、Sandbox/TestFlight、服务端对账、退款事件与任何广告 SDK/同意流仍是独立闸门。
- 当前验证：Windows 静态检查、155 个 Swift 文件的 AST 语法解析、project/plist 配置解析和 OpenAPI 校验通过；固定版本 Redocly 2.12.0 以退出码 0 确认描述有效，并报告 12 条既有非阻断 4xx 风格警告。
- 当前真实边界：未接入获授权后端；`SPORTS_DATA_MODE` 默认仍为 `mock`，占位域名不会被宣称为真实集成。
- 未通过的硬闸门：尚未在 macOS/Xcode 中编译、运行模拟器或进行真机 VoiceOver/Widget/Live Activity 验证。

## 0. 开工前决策闸门

必须先确认：

1. **市场与语言**：默认沙特/MENA；Arabic-first + English。
2. **产品身份**：最终品牌名、Logo 和自有主色；不能沿用 GOAT/Koora/SEC/Jdwal 资产。
3. **设备范围**：建议最低 iOS 17.0、iPhone 优先；iPad 后续适配。
4. **发布环境**：Mac/Xcode、Apple Developer 账号和至少一台真机的可用方式。
5. **数据与内容**：体育数据供应商沙盒、目标赛事清单、Logo/图片/新闻/视频权利。
6. **商业模式**：核心功能保持免费；StoreKit 支持订阅源码失败关闭，广告默认关闭；真实产品、法务链接、Sandbox/TestFlight、服务端对账与广告治理分别通过后才能发布。

验收：形成一页 `DECISIONS.md`，所有未决项有负责人和截止阶段。

## 1. 工程骨架与设计系统

交付：

- 建立 iOS 工程、单元测试、UI 测试、Widget Extension 和 Live Activity Extension。
- 建立 `DesignSystem`：颜色、字体、间距、圆角、图标、卡片和加载/空/错误状态。
- 建立 Arabic/English 文案、RTL/LTR 镜像、动态字体和 VoiceOver 约定。
- 建立本地 JSON fixtures 和可切换的 `MockSportsDataProvider`。

建议目录：

```text
SportsHub/
  App/
  DesignSystem/
  Core/
    Networking/
    Persistence/
    Localization/
    Analytics/
  Features/
    Onboarding/
    Home/
    Matches/
    MatchCenter/
    Explore/
    Following/
    Profile/
    News/
    Video/
  Extensions/
    Widgets/
    LiveActivity/
  Tests/
```

验收：应用离线启动；两种语言切换后无裁切；所有根页面有加载、空、错误、内容四态；测试能在 macOS/Xcode 环境运行。

## 2. 第一条纵向切片

只实现一个完整用户故事：

> 用户首次启动选择阿拉伯语，关注 Al Hilal（仅样例数据），在首页看到其下一场比赛，进入比赛中心查看事件、阵容和统计，开启提醒后在模拟器/真机收到测试更新。

交付：

- 引导与关注选择。
- 首页重要比赛条 + 一条新闻卡。
- 日期比赛列表。
- 比赛中心：摘要、事件、阵容、统计。
- 本地收藏和最近成功缓存。
- 模拟实时事件源与通知状态。

验收：该故事从冷启动到结束无死路；关闭网络后仍可重开最近内容；UI 测试覆盖 Arabic RTL 和 English LTR 两条路径。

## 3. 后端契约与真实体育数据

交付：

- 定义 OpenAPI；至少包含：
  - `GET /v1/home`
  - `GET /v1/fixtures?date=&competition=&live=`
  - `GET /v1/fixtures/{id}`
  - `GET /v1/fixtures/{id}/events`
  - `GET /v1/fixtures/{id}/lineups`
  - `GET /v1/fixtures/{id}/statistics`
  - `GET /v1/competitions/{id}/standings`
  - `GET /v1/search`
  - `GET/POST /v1/me/follows` 与 `DELETE /v1/me/follows/{followId}`
- 建立外部数据 Provider Adapter、内部 ID 映射、重试、去重、纠错与过期策略。
- 用一个真实赛事沙盒替换样例比赛，同时保留 mock 模式。

验收：供应商重复/乱序/纠错事件测试通过；客户端不认识供应商 ID；数据源故障时返回最近成功快照和明确的新鲜度。

## 4. 新闻、搜索与运营台

交付：

- 文章列表/详情、来源、作者、时间、分享、收藏。
- 原创结构化 Visual Brief：双语指标、两项对比、序列、可见数据说明，以及旧收藏格式迁移；真实 CMS 编辑器和获授权统计源仍待接入。
- CMS：草稿、审核、定时发布、更正、撤回、专题和推送预览。
- 全局搜索：赛事、球队、球员、新闻、视频；阿拉伯文正规化和英文别名。
- 首页规则推荐：关注匹配、新鲜度、运营优先级，并记录推荐原因。

验收：撤回内容在客户端失效；更正有版本痕迹；未授权媒体不能被编辑发布；搜索对常见 Arabic 拼写变体有测试。

## 5. 通知、Live Activities 与 Widget

交付：

- APNs 令牌注册、用户级通知规则、去重、限流和时区处理。
- 进球、红黄牌、阵容、半场、全场和突发新闻模板。
- Live Activity 展示比分、时钟和最新事件。
- 下一场/今日比赛 Widget。

验收：同一事件不会重复推送；纠错不会产生误导性第二条“新进球”；结束比赛自动关闭 Live Activity；关闭权限后 App 内状态准确。

## 6. 深度足球数据与离线

交付：

- 赛事详情、赛季选择、积分榜、射手/红黄牌榜、H2H、历史赛季。
- 球队、球员、阵容、转会中心、重要赛历、播出信息。
- 缓存预算、过期清理、弱网重试、离线标识和手动清除。

验收：排名规则与供应商结果一致；时区/夏令时测试通过；离线数据永远显示最后更新时间；清除数据后敏感缓存和账号令牌均被移除。

## 7. 视频点播与内容发现

交付：

- Explore 分类、视频详情、HLS 播放、字幕/音轨、收藏、观看历史和继续观看。
- 直播、回放、集锦、原创节目统一内容模型。
- CDN 签名 URL、地区/授权窗口/登录限制和播放错误状态。

验收：无权内容无法通过直接 URL 绕过；网络切换时播放器恢复合理；观看进度跨设备一致；字幕、VoiceOver 和方向控制可用。

## 8. 社区、预测与商业化

这些模块逐项上线，不能打包成一次高风险发布：

1. 反应/评论、举报和作者屏蔽的客户端/API 源码已建立；已部署过滤与审核队列、封禁/申诉后台、公开支持联系人、处理 SLA 和 App Store 审核证据仍待完成，生产开关保持关闭。
2. 免费非博彩的小组完整排序源码已建立：公共发现、服务端锁定、账户私有读取/保存和无手势排序已冻结；真实计分、反作弊、参赛资格、地区限制与法务审核仍未实现。
3. StoreKit 2 本机验证、购买、恢复和系统管理源码已建立；App Store Connect/Sandbox/TestFlight 与服务端权益对账、退款事件仍待完成。
4. 广告仅在同意、隐私标签和儿童/地区规则明确后启用。

验收：Apple UGC 条款、隐私政策、账号删除、订阅披露和审核后台全部准备好后才可开关功能。

## 9. 发布与质量闸门

- 单元测试：时间/比分状态机、排名、缓存、事件去重、通知规则、权益。
- 合约测试：App 与 OpenAPI；供应商适配器 fixture 回放。
- UI 测试：首启、关注、首页、比赛中心、文章、视频、离线、账号删除。
- 可访问性：VoiceOver、动态字体、对比度、减少动态效果、RTL 焦点顺序。
- 性能：冷启动、首页首屏、长列表滚动、比赛事件高峰、视频首帧。
- 安全：凭证、日志脱敏、深链、媒体 URL、后台权限、依赖与隐私清单。
- TestFlight：内部 → 小规模外部 → 比赛日压力测试 → App Store。

## 10. 下一步

1. 在 macOS/Xcode 上生成工程并完成第一次类型检查、单元测试和 iPhone 模拟器回归。
2. 修复由 Apple SDK/Swift 6 严格并发检查暴露的问题，再冻结第一个可运行 checkpoint。
3. 获得体育数据供应商沙盒和内容授权后，先实现服务端 Provider Adapter 与契约测试，再把 `SPORTS_DATA_MODE` 切到 `remote`。
4. 单条历史删除、账户永久删除、本机游客数据清除、公共缓存盘点/清除、公共数据状态、比赛中心增量轮询、排名/H2H、阵容/阵型与统计、比赛时刻/赛后报道、地区化播出指南、分享/深链、文章收藏、结构化 Visual Brief 与授权主图、Following 聚合、多实体关注、情境化提醒入口、多兴趣首启引导、首页可解释聚合、新闻发现、首页比赛状态、Matches 深度发现、Arabic-first 全局搜索、赛事历史赛季档案与逐赛季赛程、赛事/球员近期内容、载荷驱动视频发现、视频详情编辑上下文、球队详情上一场/下一场与权威关联新闻/视频、免费非博彩小组排名预测，以及真实快照 Widget/本地 Live Activity 生命周期均已完成源码与静态回归。正式品牌域名、政策 URL、发布主体和法定留存文本仍需用户提供，不能由客户端虚构；下一硬闸门应优先转到 macOS/Xcode 的类型检查、XCTest、签名和真机系统表面验证。Universal Links、远程通知/ActivityKit 推送和 FairPlay 分别等待真实域名/AASA、推送后端/Apple 签名以及获授权测试流。
