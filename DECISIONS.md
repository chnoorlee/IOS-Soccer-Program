# SportsHub 暂定决策记录

> 状态：2026-08-08 暂定，可在接入真实数据和品牌资产前修改。  
> 原则：没有用户明确反对时，先采用可逆、低锁定的推荐值继续实现。

| 决策 | 暂定选择 | 原因 | 重新确认闸门 |
|---|---|---|---|
| 目标市场 | 沙特及 MENA 体育用户 | 与四款参考 App 的公开定位一致 | 真实数据采购前 |
| 语言 | Arabic-first，完整 English | RTL 是产品基础能力，不是后期翻译 | 首次设计评审 |
| 平台 | iPhone 优先，iPad 后续 | 先控制首轮布局和测试范围 | TestFlight 前 |
| 最低系统 | iOS 17.0 | 支持 SwiftUI、WidgetKit、ActivityKit 的现代实现，同时保留合理覆盖 | 第一次 Xcode 构建前 |
| 产品品牌 | `SportsHub` 仅作工程代号 | 避免未经商标检索就固化正式品牌 | 设计资产进入工程前 |
| 视觉方向 | 深海蓝 + 青色 + 暖金，自有设计系统 | 与 GOAT/Koora/SEC/Jdwal 的黑橙或荧光绿拉开距离 | 视觉评审时 |
| 首版商业化 | 核心功能免费；可选 StoreKit 支持订阅；当前默认无广告 | 覆盖 Jdwal 的公开支持/去广告任务，同时让未接 App Store Connect、法务链接和广告治理的构建失败关闭 | App Store Connect 产品、隐私/条款、广告同意与 TestFlight 审核前 |
| 数据 | Provider Adapter + 本地 fixtures | 供应商未定，避免客户端被单一 API 锁定 | 真实比赛接入前 |
| 内容资产 | 仅使用自有/占位内容 | 不复制参考 App 新闻、图片、Logo 或视频 | 每次内容导入 |
| 文章授权主图 | `ArticleSummary.heroMedia` 只接收 CMS 已确认权利的公开直链、精确 MIME/尺寸、双语替代文本与可见署名；加载器使用精确主机 allowlist、无身份会话、无重定向、流式 8 MiB 上限和原创失败占位 | 覆盖 GOAT/Koora 截图中可验证的图片新闻扫描任务，同时不把 URL 冒充许可证、不抓取参考资产，也不让签名 URL 进入个人收藏快照 | 真实 CMS 权利工作流、CDN 主机/缓存 SLA、编辑裁切、市场保留窗口和法务抽检冻结前 |
| 登录 | 首轮游客模式；可选 Sign in with Apple；登录后由用户确认合并本机状态 | 降低首次使用阻力和数据收集，避免静默覆盖两套身份数据 | 真实身份后端与 Apple 能力接入前 |
| 账户删除 | 已登录用户在 Profile 内两步确认后永久删除；204 只代表服务端已完成删除和 Apple 授权撤销 | 满足 Apple 5.1.1(v)，避免把停用、排队或本机退出冒充永久删除 | 真实后端、隐私政策与法定留存评审前 |
| 本机游客数据清除 | 隐私页明确统计并清除历史、文章/视频收藏、关注和本地视频快照；不联系服务器、不改变登录账号与设备偏好 | 让“设备数据删除”可审计，并防止把账号数据或旧关注快照误当成游客状态 | 正式隐私政策、缓存保留期与 App Privacy 申报冻结前 |
| 文章收藏与 Following | 游客只持久化收藏文章的列表元数据、ID 与时间；账户使用 `/me/article-favorites`；Following 独立展示通知、文章、视频和球队/球员/赛事关注，不推断内容与任何关注对象的关系 | 保留离线可移除能力，同时隔离身份、正文/媒体和不受证据支持的推荐关系 | 真实内容关系、跨设备同步和推荐解释规则冻结前 |
| 文章 Visual Brief | 使用双语语义文本组成指标网格、恰好两项对比或序列；格式/载荷、数量、唯一 ID 和文本边界在缓存前校验，旧收藏缺失格式迁移为普通文章 | 覆盖 Koora Break 公开描述中的信息图任务，同时避免复制私有模板、引入不可读图片或把未授权素材/统计塞进客户端 | 真实 CMS、统计供应商、来源追溯、视觉发布审核和素材授权冻结前 |
| 文章卡互动摘要 | `ArticleSummary` 携带与观看者无关的总反应数和已发布评论数；缺失隐藏，详情社区继续独立 `no-store`，卡片不嵌套分享按钮或分享次数 | 覆盖 Koora 截图中可验证的卡片互动扫描任务，同时避免把个人状态写入公共缓存、把未知当零或从本机点击制造“分享量” | 真实社区聚合、分享归因/反作弊、缓存新鲜度 SLA 与产品排序规则冻结前 |
| 多实体关注 | 以 `(type, entityId)` 为身份；游客保存最小类型快照，账户响应返回服务端权威快照；跨类型按创建时间、类型和 ID 稳定排序；关注只改变通知候选受众 | 防止相同原始 ID 冲突、N+1 详情加载、旧身份泄漏和关注即授权的误导 | 服务端实体关系或逐对象通知规则冻结前 |
| 关注球队赛程看板 | 使用公共 `/teams/match-snapshots` 批量读取；按关注顺序返回每队一个可空的上一场/下一场，HTTP 每批 20、客户端总量 100 | 直接覆盖 Jdwal 截图中最清晰的未完成页面能力，同时避免逐队 N+1 和客户端猜“最近” | 供应商最近赛程定义、球队失效策略和真实缓存 SLA 冻结前 |
| 视频编辑发现 | 使用独立 `/videos/discovery` 原子快照；sport、精选 ID 和有序 Trending ID 均由服务端明确下发，编辑位置与播放权完全正交 | 覆盖 SEC 截图中的主视觉、排名与分类，同时防止客户端用观看/收藏数据伪造热度或把展示位置误当授权 | 真实编辑 CMS、排序 SLA、图片/视频授权和推荐解释规则冻结前 |
| Arabic-first 全局搜索 | 远程 `/search` 数组顺序为唯一相关性依据；客户端只做保持子序列的类型范围，Mock 使用书面化 Arabic 正规化和四级确定性匹配 | 参考截图只证明搜索入口与跨内容任务，不公开私有排名；必须把服务端排名、演示匹配和个性化推荐分开 | 真实搜索服务、查询语料、延迟/召回指标、游标产品形态和隐私评审冻结前 |
| 视频详情编辑上下文 | `/videos/{videoId}` 显式返回可空出版方/节目和最多 10 条有序相关推荐；路径 ID、唯一性、自引用与每条播放权在缓存前校验 | 覆盖 SEC 截图中折叠简介与发布日期，并把官方描述支持的节目/推荐概念落到可审计服务端关系，而非客户端猜测 | 真实节目目录、推荐服务、隐私评审、埋点和授权媒体接入前 |
| 比赛时刻与赛后报道 | `/fixtures/{fixtureId}/content` 返回最多 10 个有序时刻和 10 篇报道；精确回显比赛 ID，时刻/视频/文章 ID 唯一，视频仍走独立播放授权 | 直接覆盖 GOAT 截图中的 `لحظات من المباراة`，同时让内容失败不影响比分和实时事件，避免按队名或标题猜关联 | 真实 CMS、媒体权利、地区窗口、供应商比赛映射和比赛日 SLA 冻结前 |
| 免费小组排名预测 | 只做无赔率、无投注、无入场费、无现金价值的完整小组排序；公共游戏可 ETag，个人提交必须登录、`no-store`、整份替换并受服务端状态/截止时间裁决 | GOAT 2.1.8 当前更新说明已明确小组排名预测；先实现可审计的免费互动，不把商业化或带奖规则偷偷带入客户端 | 真实赛事参赛资格、计分/反作弊、地区限制、条款 URL、品牌许可和法律审查冻结前 |
| 地区化播出指南 | 比赛载荷最多携带 12 条 Provider 顺序的地区、双语频道、可空双语解说和规范音频语言；比赛卡仅提示，比赛中心完整展示，绝不含 URL 或播放动作 | 覆盖 Jdwal 公开功能矩阵中的频道/解说需求，同时把“节目表元数据”和“地区权益/订阅/播放授权”严格分离 | 真实播出供应商、地区匹配、更新 SLA、频道商标和版权法务冻结前 |
| 全局转会中心 | `/transfers` 使用服务端状态过滤和最新优先游标分页；所有记录强制显示 `RUMORED`、`AGREED` 或 `COMPLETED`，演示回退只允许首屏且终止分页 | 覆盖 GOAT 转会更新和 Jdwal 转会中心任务，同时避免客户端把传闻升级为事实，或把真实页与虚构页混合 | 授权转会供应商、来源/纠错政策、球队映射和更新 SLA 冻结前 |
| 赛季重要日期 | `/season-calendar` 使用 Provider 原子窗口，事件类型、顺序、日期范围和可空赛事关系均由服务端明确下发；客户端只做即将发生/完整赛季与载荷真实类型筛选 | 覆盖 Jdwal 公开描述中的赛季重要日期，同时避免从赛程、转会或客户端时区推断日期，或混合真实与虚构来源 | 授权赛历供应商、来源/纠错政策、赛事映射、时区和更新 SLA 冻结前 |
| 赛事/球员近期内容 | 使用两个季节无关、精确回显实体 ID 的 ETag 资源；文章倒序、视频保留 Provider 编辑顺序，客户端不做文本关联推断 | 覆盖 GOAT 官方描述中的球员动态、采访、报道与视频任务，同时避免把名称匹配或本机行为包装成权威关联 | 真实 CMS 关系、编辑完整性、媒体权利、来源标记和更新 SLA 冻结前 |
| 公共内容缓存 | App 自主管理的 ETag 响应缓存限 50 MiB/200 项，按最旧写入时间淘汰；隐私页显示条目、占用、上限、更新时间并可单独清除 | 让可恢复数据的占用和删除真实可审计，同时把账号私有 `no-store` 数据、游客状态和 Keychain 留在独立边界 | 真实数据供应商新鲜度 SLA、离线产品规则与 App Privacy 申报冻结前 |
| 公共数据状态 | 首页、比赛、新闻和视频列表/详情显式区分在线更新、ETag 验证、离线快照、账户私有实时、纯演示、虚构演示回退和刷新失败；先通过解码/契约再记录成功 | 不把旧缓存、Mock 或账号私有响应混称为“实时”，并让弱网状态对视觉与 VoiceOver 用户都可理解 | 真实供应商 SLA、运营文案和离线保留窗口冻结前 |
| 比赛中心增量更新 | 完整 REST 快照负责恢复，前台以排他 revision 游标做无缓存 HTTP 增量轮询；稳定事件 ID 支持更正与 tombstone，真实 SSE 延后 | 在没有供应商沙盒、心跳和代理超时证据时先保证可验证的正确性；绝不把 Mock 事件混入真实比赛 | 供应商事件序列、SLA、SSE/WebSocket 沙盒和比赛日压测前 |
| Widget 与 Live Activity | Widget 只共享关注比赛的版本化最小公共快照；Live Activity 由用户逐场主动启动，开球前四小时内可用，以在 ActivityKit 八小时活动寿命内为比赛保留四小时；App 仅在该比赛页前台去重更新并在终场结束，当前不申请 push token | 覆盖 Jdwal Widget 与 SEC/GOAT 实时扫视任务，同时避免固定虚构比赛、跨身份残留和未接后台却声称持续实时 | Apple Developer App Group、生产 Bundle ID、ActivityKit 推送后端和真机辅助功能验证前 |
| 比赛积分榜与 H2H | 使用 `/fixtures/{id}/standings` 与 `/fixtures/{id}/head-to-head` 两个独立资源；服务端确认比赛赛季，H2H 允许跨赛事但逐场标注 | 避免客户端用 currentSeason 猜历史赛季，也避免一个上下文请求失败遮挡另一个；历史仅作描述，不伪装成预测 | 获得供应商真实赛季关联、稳定 ID、杯赛/联赛交叉样例和字段 SLA 后 |
| 分享与深链 | 六类公开实体使用同一严格路由；公开基地址为空时只分享文本，`sportshub://` 仅作安装后/测试入口；Associated Domains 延后 | 不伪造不可访问的公共 URL，也不在未控制域名和 AASA 时宣称 Universal Links 可用 | 正式品牌域名、Web 回退页和 App Store 链接确定后 |

## 当前外部条件

- 当前工作区位于 Windows。
- 本机没有 Swift、Xcode、`xcodebuild` 或 XcodeGen。
- 因此本机可以完成源代码、资源、契约和静态验证，但不能证明 iOS 编译、签名、模拟器、APNs、Widget 或 Live Activity 真机行为。
- 首次 macOS/Xcode 验证是正式发布链的硬性闸门，不能用 Windows 静态检查替代。
- 当前未配置受控公开域名、AASA 或 Associated Domains；原生路由和分享源码不能替代真实 Universal Links 验证。
- 账户删除边界按 Apple 的 [App 内账户删除说明](https://developer.apple.com/support/offering-account-deletion-in-your-app/) 与 [App Review Guidelines 5.1.1(v)](https://developer.apple.com/app-store/review/guidelines/#data-collection-and-storage) 冻结；Sign in with Apple 撤销由服务端完成，客户端不持有 Apple client secret。

## 待用户最终决定

1. 正式品牌名、Logo 和域名。
2. 是否坚持 Arabic + English，是否增加中文。
3. Apple Developer / App Store Connect 和 Mac 的可用方式。
4. 首批必须覆盖的联赛和体育项目。
5. 数据、新闻、图片、队徽、视频与直播内容的授权预算。
6. 正式隐私政策 URL、发布法律主体、联系渠道和依法必须保留的数据范围。
